services:
  redis:
    image: redis:7
    container_name: redis
    restart: always
    ports:
      - "6379:6379"
    command: redis-server --requirepass CaoKai1026
    volumes:
      - redis_data:/data

volumes:
  redis_data:
